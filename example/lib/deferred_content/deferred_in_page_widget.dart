import 'package:flutter/material.dart';

class DeferredInPageWidget extends StatelessWidget {
  const DeferredInPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Deferred In Page Widget');
  }
}
